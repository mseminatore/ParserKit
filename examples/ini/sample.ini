; sample.ini — used by the ParserKit "ini" example
# both '#' and ';' line comments are recognized

[server]
name = "primary"
host = localhost
port = 8080
enabled = true

[client]
name = "primary-client"
timeout = 30
retries = 3
